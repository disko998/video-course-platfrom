---
const points = [
    {
        icon: `<svg viewBox="0 0 24 24" fill="none" class="w-8 h-8">
            <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            <path d="M8 12h8M8 8h8M8 16h6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
        </svg>`,
        title: 'Cutting Edge Editing Skills',
        subtitle: 'Master the Art',
        desc: "It isn't just about creating polished content—it's about pushing boundaries, experimenting, and developing a style that turns heads and opens doors to exciting opportunities.",
        badge: 'Technical Mastery',
        color: 'from-blue-500 to-cyan-500',
        bgImage: 'https://images.unsplash.com/photo-1464983953574-0892a716854b?auto=format&fit=crop&w=800&q=80'
    },
    {
        icon: `<svg viewBox="0 0 24 24" fill="none" class="w-8 h-8">
            <path d="M16 4h2a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2h2" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            <rect x="8" y="2" width="8" height="4" rx="1" ry="1" stroke="currentColor" stroke-width="2"/>
            <path d="M12 11v6M9 14l3-3 3 3" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>`,
        title: 'Building Your Creator Brand',
        subtitle: 'Stand Out & Shine',
        desc: 'Building a creator brand begins with creating outstanding content, sharing your work, and refining your approach to develop a signature style that defines your brand.',
        badge: 'Brand Identity',
        color: 'from-purple-500 to-pink-500',
        bgImage: 'https://images.unsplash.com/photo-1515378791036-0648a3ef77b2?auto=format&fit=crop&w=800&q=80'
    },
    {
        icon: `<svg viewBox="0 0 24 24" fill="none" class="w-8 h-8">
            <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" stroke="currentColor" stroke-width="2"/>
            <circle cx="9" cy="7" r="4" stroke="currentColor" stroke-width="2"/>
            <path d="M23 21v-2a4 4 0 0 0-3-3.87M16 3.13a4 4 0 0 1 0 7.75" stroke="currentColor" stroke-width="2"/>
            <path d="M12 12l2 2 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>`,
        title: 'Connecting with Creators & Brands',
        subtitle: 'Network & Collaborate',
        desc: "Connecting with like-minded creators inspires new ideas, fosters collaboration, and creates opportunities you wouldn't find alone. Build relationships with brands for unique partnerships.",
        badge: 'Networking',
        color: 'from-green-500 to-emerald-500',
        bgImage: 'https://images.unsplash.com/photo-1506744038136-46273834b3fb?auto=format&fit=crop&w=800&q=80'
    },
    {
        icon: `<svg viewBox="0 0 24 24" fill="none" class="w-8 h-8">
            <path d="M12 2v20M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            <path d="M8 12h8" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
            <circle cx="18" cy="6" r="3" stroke="currentColor" stroke-width="1.5"/>
        </svg>`,
        title: "You Don't Need 100k+ Followers",
        subtitle: 'Quality Over Quantity',
        desc: "I always thought I needed hundreds of thousands of followers to get recognized. But even with 'just' 20,000 followers, building my presence attracted major brands I always wanted to work with.",
        badge: 'Mindset Shift',
        color: 'from-orange-500 to-yellow-500',
        bgImage: 'https://images.unsplash.com/photo-1465101046530-73398c7f28ca?auto=format&fit=crop&w=800&q=80'
    },
];
---

<div class='relative max-w-6xl mx-auto'>

    <div class='space-y-12 lg:space-y-16'>
        {points.map((point, index) => (
            <div class={`relative flex flex-col lg:flex-row items-center gap-8 ${index % 2 === 0 ? 'lg:flex-row' : 'lg:flex-row-reverse'}`}>
                
                <!-- Content Card -->
                <div class='flex-1 group relative'>
                    <!-- Step Number - Outside the card -->
                    <div class='absolute -top-4 left-8 z-20'>
                        <div class={`w-12 h-12 rounded-2xl bg-gradient-to-r ${point.color} flex items-center justify-center shadow-lg transform group-hover:scale-110 transition-transform duration-300`}>
                            <span class='text-white font-bold text-lg'>{String(index + 1).padStart(2, '0')}</span>
                        </div>
                    </div>

                    <!-- Badge - Outside the card -->
                    <div class='absolute -top-2 right-8 z-20'>
                        <span class={`px-3 py-1 text-xs font-medium bg-gradient-to-r ${point.color} text-white rounded-full shadow-lg`}>
                            {point.badge}
                        </span>
                    </div>

                    <div class='relative p-8 pt-12 rounded-3xl bg-white/5 backdrop-blur-xl border border-white/10 hover:border-orange-500/30 transition-all duration-500 hover:bg-white/8 hover:transform hover:scale-105'>
                        
                        <!-- Background Image Overlay -->
                        <div class='absolute inset-0 opacity-10 group-hover:opacity-20 transition-opacity duration-500 rounded-3xl overflow-hidden'>
                            <img 
                                src={point.bgImage} 
                                alt={point.title}
                                class='w-full h-full object-cover'
                                loading='lazy'
                            />
                            <div class='absolute inset-0 bg-gradient-to-br from-[#0a0a0f]/80 to-[#1a1f2e]/80'></div>
                        </div>

                        <!-- Content -->
                        <div class='relative z-10 pt-6'>
                            <!-- Icon -->
                            <div class={`w-16 h-16 rounded-2xl bg-gradient-to-br ${point.color}/20 border border-white/20 flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300`}>
                                <div class={`text-white group-hover:scale-110 transition-transform duration-300`} set:html={point.icon} />
                            </div>

                            <!-- Text Content -->
                            <div class='space-y-4'>
                                <div>
                                    <p class={`bg-gradient-to-r ${point.color} bg-clip-text text-transparent text-sm font-medium mb-2`}>
                                        {point.subtitle}
                                    </p>
                                    <h3 class='text-2xl font-bold text-white group-hover:text-orange-400 transition-colors duration-300 leading-tight'>
                                        {point.title}
                                    </h3>
                                </div>
                                <p class='text-white/70 leading-relaxed text-lg'>
                                    {point.desc}
                                </p>
                            </div>

                            <!-- Progress Indicator -->
                            <div class='mt-6 flex items-center gap-3'>
                                <div class='flex-1 h-2 bg-white/10 rounded-full overflow-hidden'>
                                    <div 
                                        class={`h-full bg-gradient-to-r ${point.color} rounded-full transition-all duration-1000 group-hover:w-full`}
                                        style={`width: ${(index + 1) * 25}%`}
                                    ></div>
                                </div>
                                <span class='text-white/50 text-sm font-medium'>
                                    Step {index + 1}/4
                                </span>
                            </div>
                        </div>

                        <!-- Hover Gradient Overlay -->
                        <div class={`absolute inset-0 rounded-3xl bg-gradient-to-br ${point.color}/5 opacity-0 group-hover:opacity-100 transition-opacity duration-500 pointer-events-none`}></div>
                    </div>
                </div>

                <!-- Visual Element -->
                <div class='flex-1 lg:max-w-md'>
                    <div class='relative group'>
                        <!-- Main Image -->
                        <div class='relative overflow-hidden rounded-3xl aspect-[4/3] border border-white/10'>
                            <img 
                                src={point.bgImage} 
                                alt={point.title}
                                class='w-full h-full object-cover transition-transform duration-700 group-hover:scale-110'
                                loading='lazy'
                            />
                            <!-- Gradient Overlay -->
                            <div class={`absolute inset-0 bg-gradient-to-br ${point.color}/20 mix-blend-overlay`}></div>
                            <div class='absolute inset-0 bg-gradient-to-t from-black/50 via-transparent to-transparent'></div>
                            
                            <!-- Floating Icon -->
                            <div class='absolute top-6 right-6'>
                                <div class={`w-12 h-12 rounded-xl bg-gradient-to-r ${point.color} flex items-center justify-center shadow-xl transform group-hover:scale-110 group-hover:rotate-12 transition-all duration-300`}>
                                    <div class='text-white' set:html={point.icon} />
                                </div>
                            </div>
                        </div>

                        <!-- Decorative Elements -->
                        <div class={`absolute -bottom-4 -right-4 w-8 h-8 bg-gradient-to-r ${point.color} rounded-full opacity-60 animate-pulse`}></div>
                        <div class={`absolute -top-2 -left-2 w-4 h-4 bg-gradient-to-r ${point.color} rounded-full opacity-40 animate-bounce delay-300`}></div>
                    </div>
                </div>
            </div>
        ))}
    </div>

    <!-- Bottom Success Indicator -->
    <div class='mt-16 text-center'>
        <div class='inline-flex items-center gap-4 px-8 py-4 rounded-2xl bg-gradient-to-r from-green-500/20 to-emerald-500/20 border border-green-500/30 backdrop-blur-sm'>
            <div class='w-12 h-12 rounded-full bg-gradient-to-r from-green-500 to-emerald-500 flex items-center justify-center'>
                <svg viewBox="0 0 24 24" fill="none" class="w-6 h-6 text-white">
                    <path d="M9 12l2 2 4-4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
            </div>
            <div class='text-left'>
                <div class='text-green-400 font-medium'>Transformation Complete</div>
                <div class='text-white/70 text-sm'>From invisible to in-demand creator</div>
            </div>
        </div>
    </div>
</div>
